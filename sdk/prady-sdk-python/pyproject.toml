[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "kryos-sdk"
version = "1.0.0"
description = "Kryos SDK Python bindings"
requires-python = ">=3.12"
dependencies = ["httpx>=0.27.0"]

[tool.setuptools]
packages = ["kryos_sdk"]
