---
description: Set up AHNIS — install the package, initialize a palace, configure MCP server, and verify everything works.
allowed-tools: Bash, Read, Write, Edit, Glob, Grep
---

Invoke the generic AHNIS skill (using the Skill tool) with the `init` command, then follow its instructions.
