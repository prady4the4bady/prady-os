# Configuration

## Global Config

Located at `~/.AHNIS/config.json`:

```json
{
  "palace_path": "/custom/path/to/palace",
  "collection_name": "AHNIS_drawers",
  "people_map": {"Kai": "KAI", "Priya": "PRI"}
}
```

| Key | Default | Description |
|-----|---------|-------------|
| `palace_path` | `~/.AHNIS/palace` | Where ChromaDB stores your drawers |
| `collection_name` | `AHNIS_drawers` | ChromaDB collection name |
| `people_map` | `{}` | Entity name → AAAK code mappings |

## Project Config

Generated by `AHNIS init` in your project directory:

### `AHNIS.yaml`

```yaml
wing: myproject
rooms:
  - backend
  - frontend
  - decisions
palace_path: ~/.AHNIS/palace
```

### `entities.json`

```json
{
  "Kai": "KAI",
  "Priya": "PRI"
}
```

Wings are auto-detected during `AHNIS init` from:
- Directory names → project wings
- Detected people in file content → person wings
- Explicit `--wing` flag on mine commands

## Identity

Located at `~/.AHNIS/identity.txt`. Plain text. Becomes Layer 0 — loaded every session.

```text
I am Atlas, a personal AI assistant for Alice.
Traits: warm, direct, remembers everything.
People: Alice (creator), Bob (Alice's partner).
Project: A journaling app that helps people process emotions.
```

::: tip
Write your identity file in first person from the AI's perspective. This becomes the AI's self-concept on wake-up.
:::

## Palace Path Override

All commands accept `--palace <path>` to override the default location:

```bash
AHNIS search "query" --palace /tmp/test-palace
AHNIS mine ~/data/ --palace /tmp/test-palace
```

The MCP server also accepts `--palace`:

```bash
python -m AHNIS.mcp_server --palace /custom/palace
```

## Environment Variables

| Variable | Description |
|----------|-------------|
| `AHNIS_PALACE_PATH` | Override palace path (same as `--palace`) |
| `MEMPAL_DIR` | Directory for auto-mining in hooks |
